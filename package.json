{
  "name": "shangtech-website",
  "version": "1.0.0",
  "private": true,
  "description": "上科官网项目 - Monorepo架构",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "postinstall": "node scripts/setup.js",
    "setup": "node scripts/setup.js",
    "predev": "node -e \"try{require('concurrently')}catch(e){console.error('错误: 依赖未安装，请先运行 npm install');process.exit(1)}\"",
    "dev": "concurrently \"npm run dev:web\" \"npm run dev:cms\" \"npm run dev:api\"",
    "dev:web": "npm run dev --workspace=web",
    "dev:cms": "npm run dev --workspace=cms",
    "dev:api": "npm run start:dev --workspace=api",
    "build": "npm run build --workspace=web && npm run build --workspace=cms && npm run build --workspace=api",
    "build:web": "npm run build --workspace=web",
    "build:cms": "npm run build --workspace=cms",
    "build:api": "npm run build --workspace=api",
    "start": "concurrently \"npm run start:web\" \"npm run start:cms\" \"npm run start:api\"",
    "start:web": "npm run start --workspace=web",
    "start:cms": "npm run start --workspace=cms",
    "start:api": "npm run start:prod --workspace=api",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {
    "@nestjs/platform-express": "^10.0.0",
    "@types/better-sqlite3": "^7.6.13",
    "@types/multer": "^2.2.0",
    "@types/uuid": "^10.0.0",
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
